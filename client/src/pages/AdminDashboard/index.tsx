import * as React from 'react';
import AdminDashboardUI from './ui';

const AdminDashboard = () => {
    // const [state, handlers] = useAdminDashboard();

    return (
        // <AdminDashboardUI {...state} {...handlers} />
        <AdminDashboardUI />
    )
}

export default AdminDashboard;